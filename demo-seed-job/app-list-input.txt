# Inputs in two columns only
# First column for Service name
# Secound column for Bitbucket URL
# Please all more lines with same format.
# More column can be add, then need to modify generate-DSL-scripts accordingly.

service1  service1-git-repo
service2  service2-git-repo
service3  service3-git-repo
custom-service  custom-service-repo
